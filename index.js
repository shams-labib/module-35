const arrays = ['abul', 'khabul', 'gabul', 'habul'];
// arrays.push('mal')
// arrays.pop()
// arrays.unshift('bd')
// console.log(arrays.slice(1,3))

const object = {
    name: 'mal',
    age: 18,
    friends: arrays
}

console.log(object.friends[0]);